diff --git a/jdk/make/lib/NioLibraries.gmk b/jdk/make/lib/NioLibraries.gmk
index 6c9c46a3..104bece9 100644
--- a/jdk/make/lib/NioLibraries.gmk
+++ b/jdk/make/lib/NioLibraries.gmk
@@ -174,6 +174,12 @@ ifeq ($(OPENJDK_TARGET_OS_API), posix)
     ifeq ($(OPENJDK_TARGET_CPU_ARCH), ppc)
       SCTP_WERROR :=
     endif
+    # This is the only place jdk8 promotes warnings to errors, and it has no
+    # --disable-warnings-as-errors to switch off. clang on a non-macosx target means a cross build,
+    # where warnings come from the target platform's headers rather than from this source.
+    ifeq ($(TOOLCHAIN_TYPE), clang)
+      SCTP_WERROR :=
+    endif
 
     $(eval $(call SetupNativeCompilation,BUILD_LIBSCTP, \
         LIBRARY := sctp, \
